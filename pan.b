	switch (t->back) {
	default: Uerror("bad return move");
	case  0: goto R999; /* nothing to undo */

		 /* PROC B */
;
		;
		
	case 4: // STATE 2
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC A */
;
		;
		
	case 6: // STATE 2
		;
		p_restor(II);
		;
		;
		goto R999;
	}

